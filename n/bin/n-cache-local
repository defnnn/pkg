#!/usr/bin/env bash

function main {
	set -efu

    cat | xargs -n 50 | while read -r a; do
        nix copy --derivation --verbose --to 'file:///tmp/cache/nix?compression-level=0&parallel-compression=true' $a
    done
}

source sub "$0" "$@"
