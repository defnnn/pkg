#!/usr/bin/env bash

function main {
	set -efu

    cat | xargs -n 300 | while read -r a; do
        echo $a | nix copy --derivation --verbose --to 'file:///tmp/cache/nix?compression-level=0&parallel-compression=true'
    done
}

source sub "$0" "$@"
